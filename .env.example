DB_ENGINE=mysql

# Django settings
SECRET_KEY=replace-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# MySQL defaults for local usage
DB_NAME=taskmanager
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306

# SQLite fallback (optional)
# To use SQLite instead, set DB_ENGINE=sqlite and comment out the MySQL values

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
