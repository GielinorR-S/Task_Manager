# Project-level environment variables used by backend/config/settings.py
# Prefer backend/.env for local overrides; this file keeps defaults aligned with
# the sample backend/.env.example so running the backend works without a database
# service.
SECRET_KEY=replace-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Default to SQLite for local setup
DB_ENGINE=mysql
DB_NAME=taskmanager
DB_USER=taskuser
DB_PASSWORD=your_password
DB_HOST=127.0.0.1
DB_PORT=3306

# CORS
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
